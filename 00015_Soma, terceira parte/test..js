describe("", function(){
  it("lucroTotal4([2, 3, 4, 5])", function() {
  	assert.equal(lucroTotal4([2, 3, 4, 5]), 14)
  })
 
  it("lucroTotal4([2, 3, 4, 0])", function() {
  	assert.equal(lucroTotal4([2, 3, 4, 0]), 9)
  })
})

